import java.util.ArrayList;
import java.util.List;

//TIP To <b>Run</b> code, press <shortcut actionId="Run"/> or
// click the <icon src="AllIcons.Actions.Execute"/> icon in the gutter.
public class Main {
    public static void main(String[] args) {
        //TIP Press <shortcut actionId="ShowIntentionActions"/> with your caret at the highlighted text
        // to see how IntelliJ IDEA suggests fixing it.
        System.out.printf("Hello and welcome!");

        for (int i = 1; i <= 5; i++) {
            //TIP Press <shortcut actionId="Debug"/> to start debugging your code. We have set one <icon src="AllIcons.Debugger.Db_set_breakpoint"/> breakpoint
            // for you, but you can always add more by pressing <shortcut actionId="ToggleLineBreakpoint"/>.
            System.out.println("i = " + i);
        }
    }
}

public class circles{

    public boolean notInGroups(){
        // check if the position in grind isnt in the array
        return true;
    }
    public boolean hasCircle(){
        // check if the position has a check in the grid
        return true;

    }

    public static checkNeighbors(int[][] position, grid){

    }

    int[][] preset_grid;
    int[][] grid = preset_grid;

    List<int[][]> initial = new ArrayList<>();
    List<int[][]> groups = new ArrayList<int[][]>;

    for(int i = 0; i = grid.; i++){
        for (int j = 0; j = 0; j++){
            if(grid[i][j].hasCircle()){
                if(grid[i][j].notInGroups())
                    groups.add(grid[i][j])
                    checkNeighbors(grid[i][j], grid);
            }

            }
    }

}